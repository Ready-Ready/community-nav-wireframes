# Shared assets

Use this folder for project-wide images, logos, fonts, and icons that should be reused across wireframes.

Place production-ready assets here and reference them from wireframes using relative paths (e.g. `../assets/logo.svg`).
