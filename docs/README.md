# Docs (GitHub Pages)

Put final HTML files here to publish via GitHub Pages. To publish from the `main` branch, place the site root files directly in `docs/`.

Example: copy `wireframes/landing-page/index.html` and any CSS/images into `docs/` and push to `main`.
